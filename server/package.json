{
  "name": "mern-shopping",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "build": "dotenv -e .env.production -- vite build",
    "circular": "madge --circular ./src/index.tsx",
    "dev": "dotenv -e .env.development -- vite",
    "extract-translations": "i18next 'src/**/*.{ts,tsx}'",
    "jest": "jest",
    "lint": "eslint .",
    "lint-fix": "npm run lint -- --fix",
    "preview": "dotenv -e .env.development -- vite preview",
    "start": "serve ./dist",
    "test": "pnpm run tsc && pnpm run lint && pnpm run jest",
    "tsc": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.6.2",
    "dotenv": "^16.0.3",
    "express": "^4.17.2",
    "fullstackportfolio": "file:..",
    "graphql": "^16.3.0",
    "langchain": "^0.0.75",
    "mongoose": "^7.0.2",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.3",
    "prettier": "^2.7.1"
  }
}
